.venv/
.git/
node_modules/
design/
media/
static/
staticfiles/
__pycache__/
db_backups/
*.pyc
.env
.env.*
!.env.example
.pytest_cache/
.ruff_cache/
.idea/
.DS_Store
# Compiled gettext catalogs — always rebuilt from the committed .po sources
# by `RUN python manage.py compilemessages` in the Dockerfile, so a stale
# local .mo (e.g. forgotten after editing a .po) can never leak into the image.
*.mo

# Django 5.2 (the Django 5.x LTS) + django-unfold>=0.91: django-unfold<=0.90
# crashes with a ValueError inside its {% component %} template tag on every
# admin add/change form when paired with Django 5.0.x (changelist pages were
# fine, only add/edit forms hit it) — 0.91 is the first release that fixes
# it. Keep Django pinned below 6.0: CLAUDE.md's stack is "Django 5.x", and
# django-unfold has no upper Django bound, so an unpinned `pip install` here
# would silently pull in Django 6.
Django>=5.2,<6.0
psycopg[binary]>=3.1
django-unfold>=0.91
django-modeltranslation>=0.19
python-telegram-bot>=22.0
django-ratelimit>=4.1
Pillow>=12.0
django-environ>=0.14
gunicorn>=26.0
